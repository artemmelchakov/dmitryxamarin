<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App3.ContactPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Дата" />
            <Entry Text="{Binding Date}" />
            <Label Text="Стоимость" />
            <Entry Text="{Binding Offer}" />
            <Label Text="Название девайса" />
            <Entry Text="{Binding NameDevice}" />
            <Label Text="Адрес" />
            <Entry Text="{Binding Address}" />
            <Label Text="Дополнительная информация" />
            <Entry Text="{Binding Message}" />
            <StackLayout Orientation="Horizontal">
                <Button Text="Сохранить" Clicked="SaveContact" />
                <Button Text="Удалить" Clicked="DeleteContact" />
                <Button Text="Отмена" Clicked="Cancel" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>